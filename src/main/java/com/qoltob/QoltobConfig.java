package com.qoltob;

import net.runelite.client.config.Config;
import net.runelite.client.config.ConfigGroup;

@ConfigGroup("qoltob")
public interface QoltobConfig extends Config
{
    //TODO add options for 1. Show total stalls 2. Show all specific stalls. 2a if 2 on, collapse or not. 3. big splits.
}
